# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state. It changed no source code, config, tests, or lockfiles, and ran no builds or tests. Its purpose is to give any reviewer who finds this file a signed, auditable record of what was checked and what changed.

This is the **second** truth-up pass for this repo. The first ran on 2026-05-30 against HEAD `3bb34a1` and fixed CLAUDE.md (phase, stack items, LZ4 constant, CI system), docs/PORTFOLIO-DISPOSITION.md (branch/release-state rows), and created the original DOC-RECONCILIATION.md. The two commits since that pass (`7b5e84d docs: reconcile documentation to current code state`, `7b6b940 docs: lean CLAUDE.md (claude-md-lint)`) are both doc-only. This pass checks whether those changes or any older drift left anything inaccurate.

---

## Per-claim findings

### 1. What it is

**Status: `consistent`**
**Evidence: `verified-by-reading-code`**

- Entry point: `terroir-ios/Terroir/App/TerroirApp.swift` (`@main struct TerroirApp`)
- All six features listed in the README are fully implemented:
  - Interactive 3D globe: `terroir-ios/Terroir/Views/GlobeView.swift`, `GlobeScene.swift`
  - Radar chart: `terroir-ios/Terroir/Views/RadarChartView.swift`
  - Overlay modes (soil / climate / vegetation): `terroir-ios/Terroir/App/AppState.swift:160` (`setOverlay`), overlay PNGs in Resources
  - Prebuilt binary grid: `terroir-ios/Terroir/Resources/terroir.bin` present; `FlavorLookup.swift` loads and decodes it
  - Share card: `terroir-ios/Terroir/Views/ShareCardView.swift` — full `ImageRenderer` implementation, not a stub
  - "My Location": `terroir-ios/Terroir/Services/LocationService.swift`, wired in `AppState.goToMyLocation()`
- README intro paragraph is accurate.

No changes to README "What it is" content.

---

### 2. Current state

**Status: `consistent`**
**Evidence: `verified-by-reading-code`**

CLAUDE.md Portfolio Context states: "Phases 0–3 complete; current phase: Phase 4 — TestFlight + App Store Submission." Code confirms:

- Phase 0 complete: `data-pipeline/` scripts present; `terroir-ios/Terroir/Resources/terroir.bin` bundled
- Phase 1 complete: `GlobeView.swift` and `GlobeScene.swift` fully implemented with pan/tap/pinch gestures
- Phase 2 complete: `FlavorLookup.swift`, `FlavorVector.swift`, `TemplateEngine.swift`, `OceanHandler.swift`, `FlavorCardView.swift`, `RadarChartView.swift` — all implemented, none stubbed
- Phase 3 complete: `ShareCardView.swift` renders 1080×1080 via `ImageRenderer`; `EnrichmentService.swift` wired (serving bundled mock data for 5 well-known locations; live backend not yet deployed — noted as `unverifiable` below); overlay toggle wired
- Phase 4: `APPSTORE-METADATA.md` still absent (confirmed by file search) — consistent with PORTFOLIO-DISPOSITION.md noting this as the active blocker

No changes made.

---

### 3. Stack

**Status: `consistent`**
**Evidence: `verified-by-reading-code`**

All stack entries verified against code and config (same findings as 2026-05-30 pass; the lean commit did not alter these):

| Entry | Doc claims | Code confirms | Source |
|---|---|---|---|
| iOS deployment target | iOS 17.0+ | iOS 17.0 | `terroir-ios/project.yml:5` |
| Swift version | 5.10+ | 5.10 | `terroir-ios/project.yml:10` (`SWIFT_VERSION: "5.10"`) |
| FlatBuffers | 25.x | 25.12.19 | `Package.resolved:10` |
| LZ4 mode | COMPRESSION_LZ4_RAW | `COMPRESSION_LZ4_RAW` | `terroir-ios/Terroir/Engine/FlavorLookup.swift:68` |
| CI | GitHub Actions | GitHub Actions | `.github/workflows/ci.yml` |
| Globe | SceneKit | SceneKit | `GlobeView.swift:1` |
| Data pipeline | Python 3 + NumPy | Python scripts in `data-pipeline/` | `data-pipeline/requirements.txt` |

Enrichment backend (`CloudKit Functions / Vercel Edge Functions`): `EnrichmentService.swift:97` comment says "In production, this would call CloudKit Functions or Vercel Edge Functions." The actor currently serves bundled mock data only. The stack entry describes planned architecture — marked `unverifiable` (forward-looking intent); text left unchanged.

No changes made.

---

### 4. How to run

**Status: `consistent`**
**Evidence: `verified-by-reading-code`**

- README Quick Start: `xcodegen generate` then `open Terroir.xcodeproj`
- `terroir-ios/project.yml` exists (XcodeGen config)
- `terroir-ios/Terroir.xcodeproj` exists
- `terroir-ios/Terroir/Resources/terroir.bin` present — no pipeline run required
- Prerequisites "Xcode 15+, iOS 17.0+" consistent with `project.yml:5` (iOS 17.0, which requires Xcode 15+)

No changes made.

---

### 5. Known risks / Do NOT

**Status: `consistent`**
**Evidence: `verified-by-reading-code`**

CLAUDE.md "Known Risks" items checked against code:
- "Do not use UIKit directly" — `GlobeView.swift` wraps `SCNView` in `UIViewRepresentable`; all other UI is SwiftUI ✓
- "Do not block the UI thread for flavor lookups" — `FlavorLookup` is an `actor`; `AppState.handleGlobeTap` uses `async/await` ✓
- "Do not use localStorage, UserDefaults, or CoreData" — no such usage found ✓
- "Do not add third-party analytics" — `Package.resolved` shows only `flatbuffers`; no analytics packages ✓
- "Do not use bilinear interpolation" — only nearest-neighbor index math in `FlavorLookup.lookup` ✓

No changes made.

---

### 6. Next move

**Status: `consistent`**
**Evidence: `verified-by-reading-code`**

"Phase 4: TestFlight + App Store Submission" is the stated current phase. `APPSTORE-METADATA.md` is absent (searched, not found). This is consistent with PORTFOLIO-DISPOSITION.md calling it the active blocker. No additional stubs or TODOs were found in the Phase 3 code that would push the effective state back to an earlier phase.

No changes made.

---

## Drifted items fixed

### `docs/PORTFOLIO-DISPOSITION.md`

**Status: `drifted` → fixed (1 item)**
**Evidence: `verified-by-reading-code` (git log)**

The "Last known reference" table stored the `origin/main` tip SHA from the previous portfolio audit. Two doc-only commits landed since then, advancing the tip:

- `docs/PORTFOLIO-DISPOSITION.md:226` — `origin/main` tip was `3bb34a1 docs(context): recover portfolio context (#11)` → updated to `7b6b940 docs: lean CLAUDE.md (claude-md-lint)` (current HEAD)

This is a snapshot field; keeping it current lets future passes and operators know the last-verified state.

---

## Contradictions for manual review

The following files are NOT in the editable set (`README.md`, `CLAUDE.md`, `AGENTS.md`, `DOC-RECONCILIATION.md`, `docs/`). Their drift is documented here for a human to fix.

### `IMPLEMENTATION-ROADMAP.md` — carried forward from 2026-05-30 pass (still unresolved)

1. **`IMPLEMENTATION-ROADMAP.md:98`** — File structure diagram lists `data-pipeline/README.md` ("How to regenerate terroir.bin from scratch"), but this file does not exist. Fix: create `data-pipeline/README.md` or remove the entry.

2. **`IMPLEMENTATION-ROADMAP.md:75`** — File structure shows `globe_base.png`. Actual bundled resource is `terroir-ios/Terroir/Resources/globe_base.jpg` (`.jpg`). Fix: change `.png` → `.jpg` in the diagram.

3. **`IMPLEMENTATION-ROADMAP.md:475`** — Phase 0 Risks says lz4_frame format is required; actual `05_encode_binary.py` uses `lz4.block` and iOS side uses `COMPRESSION_LZ4_RAW` (block, not frame). Fix: update the risk note to say lz4 block is used consistently on both sides.

4. **`IMPLEMENTATION-ROADMAP.md:368`** — Dependencies section pins `flatbuffers==23.5.26`. Actual version is `25.12.19` (Package.resolved). Fix: update the pip install example to `flatbuffers==25.12.19`.

### `IMPLEMENTATION-ROADMAP.md` — new findings in this pass

5. **`IMPLEMENTATION-ROADMAP.md:197-205`** — Data Model section shows `EnrichmentData` struct with fields `dominantCrop`, `waterSource`, `waterMineralCharacter`, `pairingRegion`, `pairingSuggestion`, `localNote`. Actual `terroir-ios/Terroir/Models/EnrichmentData.swift:3-9` has: `soilDescription`, `climateDescription`, `vegetationDescription`, `geologicalHistory`, `nearbyAppellations`, `culturalContext`. Fix: replace the struct definition in the roadmap with the actual fields.

6. **`IMPLEMENTATION-ROADMAP.md:334-342`** — API response shape JSON example also uses the old field names (`dominantCrop`, `waterSource`, etc.). Fix: update the JSON example to match the actual `EnrichmentData` struct fields.

7. **`IMPLEMENTATION-ROADMAP.md:587`** — Phase 3, Task 1 acceptance criteria says `dominantCrop == "Pinot Noir grape"`. Since `dominantCrop` no longer exists in the struct, this criterion is stale. Fix: update to reference `soilDescription` or another actual field, e.g. `soilDescription != nil`.

---

## Footer

**Run date/time:** 2026-06-02 19:54:59 PDT
**Branch:** `docs/truth-up-2026-06-02`
**HEAD sha reconciled against:** `7b6b94086a5dfaba4bb2794f5e65a15eab562111`

**Files changed:**
- `docs/PORTFOLIO-DISPOSITION.md` — 1 edit (origin/main tip SHA updated)
- `DOC-RECONCILIATION.md` — overwritten (this file)
